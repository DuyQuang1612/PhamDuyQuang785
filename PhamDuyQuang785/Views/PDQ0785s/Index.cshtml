@model IEnumerable<PhamDuyQuang785.Models.PDQ0785>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PDQName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PDQgender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PDQName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PDQgender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PDQid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PDQid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PDQid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
